using GraphLabs.CommonUI.Controls;

namespace GraphLabs.Tests.UI
{
    /// <summary> Матрица расстояний </summary>
    public partial class DerivedMatrix : Matrix
    {
        //protected override void OnMatrixChanged(ObservableCollection<MatrixRowViewModel<string>> dataSource)
        //{
        //    base.OnMatrixChanged(dataSource);

        //    var btnColumn = new DataGridTextColumn
        //    {
        //        CellStyle = (Style)Resources["BtnCellTemplate"],
        //        Binding = new Binding("[0]"),
        //        Width = new DataGridLength(MatrixDataGrid.RowHeight)
        //    };
        //    MatrixDataGrid.Columns.Add(btnColumn);
        //    MatrixDataGrid.ItemsSource = dataSource;
        //}
    }
}
